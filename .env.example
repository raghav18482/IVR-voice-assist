# Base URL of the deployed FastAPI backend, e.g. https://cimet-voice-agent.onrender.com
# Leave blank for local dev (Vite proxies /api and /ws to http://127.0.0.1:8000, see vite.config.js)
# or when the frontend is served from the same origin as the backend.
VITE_API_BASE_URL=
